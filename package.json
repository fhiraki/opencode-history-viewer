{
  "name": "opencode-viewer",
  "version": "0.1.0",
  "description": "ローカル実行可能な OpenCode 履歴ビューワー（全文検索・タイムライン・統計）",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "lint": "biome check src public",
    "format": "biome check --write src public",
    "check": "biome check src public && node --check src/server.js && node --check src/db.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=22.5"
  },
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^2.5.14"
  }
}
